# Cloudflare Pages ставит зависимости через npm, а @dualmark/astro пока
# объявляет peer astro@^6 при astro 7 в проекте. Bun такой конфликт
# пропускает, npm без этого флага падает с ERESOLVE на сборке.
legacy-peer-deps=true
